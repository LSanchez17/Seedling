
const Dashboard = () => {
    //pings the server upon loading, getting user information

    return(
        <div>
            <p>I'm the dashboard!</p>
        </div>
    );
};

export default Dashboard;