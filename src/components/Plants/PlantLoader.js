
const PlantLoader = ({plantList}) => {
    //this handles the display as we scroll

    return(
        <div>
            <p>Loading plants here, as you scroll it loads a next chunk</p>
            <p>Clicking on the plant Picture loads the plantDetails Page</p>
            <p>Maybe just load onto page plantForm? with extra buttons?</p>
        </div>
    );
};

export default PlantLoader;